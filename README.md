# Antenna-Panels
Production Management System to control the flow in the factory
Pasos previos para la ejecución

    Instala las dependencias necesarias ejecutando este comando en tu terminal o consola de comandos:

pip install streamlit pandas


2. **Guarda el código:** Copia el código de abajo y guárdalo en un archivo llamado `app.py`.

3. **Ejecuta la aplicación:** Corre el siguiente comando en tu terminal:
   ```bash
   streamlit run app.py
